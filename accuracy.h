#include <iostream>

using namespace std;

float accuracy() {
    leave_one_out_cross_validation(data, current_set, feature_to_add)
        accuracy = rand;

    end
}  //leave_one_out_cross_validation()